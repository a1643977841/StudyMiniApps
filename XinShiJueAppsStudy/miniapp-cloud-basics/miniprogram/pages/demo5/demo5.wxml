<view>
  <view bindtap="clickRow" data-id="{{item._id}}" data-idx="{{index}}" wx:for="{{dataList}}" wx:key="index" class="row">
    <view class="title">{{item.title}}</view>
    <view>{{item.hits}}</view>
  </view>
</view>